{
  "devDependencies": {
    "axios": "^0.27.2",
    "jest": "^27.5.1"
  },
  "scripts": {
    "test": "jest",
    "debug": "nodemon app.js",
    "start": "docker run -p 3000:3000 mptestserver",
    "build": "docker build -t mptestserver ."
  },
  "dependencies": {
    "@mparticle/cli": "^1.0.8",
    "dotenv": "^16.0.1",
    "fastify": "^3.29.0",
    "node-cache": "^5.1.2"
  }
}
